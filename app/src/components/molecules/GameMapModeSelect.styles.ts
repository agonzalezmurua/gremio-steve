import tw from 'twin.macro';

const Item = tw`cursor-pointer bg-white flex flex-row items-center h-8 space-x-1 px-2 hover:(bg-gray-300)`;

const MapModeSelectStyles = {
  Item: Item,
};

export default MapModeSelectStyles;

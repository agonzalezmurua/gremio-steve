import tw from 'twin.macro';

const Wrapper = tw`flex bg-white rounded border items-center`;
const Input = tw`bg-transparent px-2 text-sm flex-grow h-8`;
const Icon = tw`p-2`;

const InputStyles = {
  Wrapper: Wrapper,
  Input: Input,
  Icon: Icon,
};

export default InputStyles;

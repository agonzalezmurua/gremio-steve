import React from 'react';
import ExternalWindow from '_components/molecules/external-window';

const LoginPage = () => {
  return (
    <ExternalWindow>
      <div>This should be the callback window</div>
    </ExternalWindow>
  );
};

export default LoginPage;

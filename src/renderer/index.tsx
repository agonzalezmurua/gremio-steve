import React from 'react';
import ReactDOM from 'react-dom';
import Document from '_/Document';
import App from '_/App';

const root = document.getElementById('root');

ReactDOM.render(
  <Document>
    <App />
  </Document>,
  root
);
